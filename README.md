# Integrate Alacritty with Nautilus.

### Installation
0. `sudo dnf remove gnome-terminal-nautilus`  
1. `sudo dnf install nautilus-python`  
2. Copy the .py file to `/usr/share/nautilus-python/extensions`
